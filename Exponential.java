
class Exponential { //compute the result of the first number given to the power of the second number given.
    public double computeExponent(int number, int exponent) {
        return Math.pow(number, exponent);
    }    
}